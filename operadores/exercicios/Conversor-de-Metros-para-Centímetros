"""Peça ao usuário digitar um valor em metros e converta para centímetros (1 metro = 100 cm)."""

metros=float(input("Entre com o valor em metros: "))

centimetros= metros*100

print("Você precisará, em centimetros : {} cm".format(centimetros))